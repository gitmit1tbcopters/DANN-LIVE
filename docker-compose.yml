services:
  dann-lab:
    build: .
    ports:
      - '5173:5173'
      - '8931:8931'
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
